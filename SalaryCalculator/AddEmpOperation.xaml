<Window x:Class="SalaryCalculator.AddEmpOperation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SalaryCalculator"
        mc:Ignorable="d"
        Title="Add Operation" MinHeight="350" MinWidth="600" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.8*"></RowDefinition>
            <RowDefinition Height="0.2*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBox Grid.Column="0"></TextBox>

        <WrapPanel Grid.ColumnSpan="5" Grid.Row="1" HorizontalAlignment="Center" Margin="0, 10, 0, 0">
            <Button Content="OK" FontSize="14" MinWidth="60" Margin="0, 0, 20 ,0" IsDefault="True" ></Button>
            <Button Content="Cancel" FontSize="14" MinWidth="60" IsCancel="True"></Button>
        </WrapPanel>
    </Grid>
</Window>
